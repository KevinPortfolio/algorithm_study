#include <stdio.h>

#include "trees.cpp"

int main()
{
  int tree_count = number_of_unqiue_binary_trees(4);
  printf("%d", tree_count);

  bool running = true;
  while (running)
  {
    
  }
  
  return 0;
}
